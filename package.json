{
  "name": "smart-motion-light-serverless",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "lint": "eslint .",
    "check:functions": "node --check netlify/functions/telegram-webhook.js && node --check netlify/functions/set-webhook.js && node --check netlify/functions/hivemq-ingest.js && node --check netlify/functions/status.js && node --check netlify/functions/health.js"
  },
  "dependencies": {
    "@netlify/blobs": "^9.0.0",
    "mqtt": "^5.10.4"
  },
  "devDependencies": {
    "eslint": "^9.22.0"
  }
}
